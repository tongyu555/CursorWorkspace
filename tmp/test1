  RDD弹性分布式数据集
弹性：RDD可以在把数据放到内存中，如果内存满了可以弹到到硬盘上去。容错的弹性，可以根据宽窄依赖重算那部分重算
分布式：RDD是由多个分区组成的数据，每个分区对应一个task，而每个task可以在不同的节点上运算，所以多个task可以组成分布式运算

spark的开发就是写RDD的依赖关系，然后可以从这个关系中分成多个stage阶段，每个stage阶段会产生shuffle

对比hadoop
只替代hadoop的mr，不能替代hadoop的hdfs
当然它有自己的Standalone模式，但是可以使用hadoop的yarn代替，在企业中推荐使用yarn模式，为什么呢？省钱提高机器的利用率。

spark VS mr
1.如果是窄依赖的计算不用等待其它分区
2.spark不需要在shuffle的过程中强制排序，在需要的时候排序就行，为什么不需要强制？因为下游可以使用内存进行数据的聚合，而MR没有使用内存聚合这样就必须强制数据连续存储在硬盘，以方便读取。
3.mr在map输出的时候会写硬盘，在reduce拉取的时候会写硬盘，而spark在map输出的时候会写磁盘，在reduce拉取的时候不会
4.可以多次使用的数据放到内存中，避免多次去读磁盘
5.spark的算子多，处理数据方法多
6.spark过度依赖内存，而mr由于数据是在硬盘一点点的读进来的所以并不需要太多内存
7.mr跑任务是用的进程，spark跑任务是用的线程


spark启动任务的流程
driver -> master -> worker -> executor -> driver -> task -> executor

RDD里面存是的真正的数据吗？不是它是抽像的，那数据存在那里啊？存在存储端比如HDFS，HBASE。RDD只是去读数据的元信息，然后分出多个分区，然后生成对应TASK，TASK再去读真正的数据
分区的计算如果是读的HDFS，那就使用FileInputFormat中的getSplit方法返回split分区信息，HDFS一个块是一个分区（确切的说是通过计算的）
分区的计算如果是读的HBASE，那就使用TableInputFormat中的getSplit方法返回split分区信息，Table表中的一个Region是就一个分区

宽窄依赖
宽依赖就是产生shuffle的依赖，其它都是窄依赖
什么是shuffle，进行数据的传输（可以通过网络也可能不通过网络）并且进行key的重新洗牌

窄依赖与宽依赖的区别，容错性
宽依赖需要重新算所有的父分区
窄依赖只需要得新算对应的父分区
遇到宽依赖就会分成不同的stage


对于所有分布式计算框架都会设计到数据本地化策略，什么是数据本地化策略，就是让任务跑在数据所在机器，这样可以避免数据的网络传输。
所以所有的分布式计算框架都采用移动计算不移动数据的方式进行数据的本地化，就是把计算的程序发到数据所在机器。

RDD分两类
Transformations:
reduceByKey是把数据按key进行分组并把values按reduce的函数进行运行，与它有相同功能的算子是groupByKey.map()
groupByKey.map()能拿到key，而reduceByKey拿不到key。
reduceByKey中在map端的输出的时候会对数据进行局部的聚合操作，而groupByKey是把数据原样拉取过来的。所以groupByKey拉取的数据量大，所以它比reduceByKey慢


coalesce和repartition这两个算子的作用都是改变rdd的分区数
那改变rdd的分区数有几种办法呢？
1.可以使用算子的第二个参数
2.可以使用全局的参数设置spark.default.parallelism
3.可以使用coalesce和repartition这两个算子

coalesce和repartition的区别
repartition必须进行shuffle,而coalesce可以选择是否shuffle，默认是不shuffle
coalesce适合从多的分区变成少的分区（注意避免激烈的变比，比如从1000个分区变成1个分区，为什么呢？因为变成1个分区那不就是等于把分布式计算任务变成单机任务了吗）
    那为什么从多变少不需要shuffle呢？因为从多变少就相当于rdd的union操作，所以窄依赖就可以完成
repartition适合从少的分区变成多的分区
    那为什么从少变多需要shuffle呢？因为从少的分区变成多的分区就设计到key的分配问题，所以必须得对key进行重新的洗牌，所以就必须得shuffle


mapPartitionsWithIndex它有一个独特的作用，什么作用呢？可以拿出task的ID，这样就可以用其实现并行的文件输出

checkpoint与cache的作用是都能保存rdd计算的中间结果，让下游可以重复的读取中间结果，避免多次计算。
那它俩的区别是什么呢？
1.ckp是把数据保存到hdfs上，cache是把数据保存到executor的内存或者worker节点的硬盘上
2.ckp能触发一次任务，cache不会
3.ckp之后driver会把ckp之前的rdd依赖关系忘记，因为数据在HDFS上了，所以就彻底安全了。而cache保存的数据可能还不安全所以不会忘记RDD的依赖关系，以用于后面的容错

cache和persist的区别
cache是默认把数据保存到内存中的，而persist可以选择存储级别


Action:
collect作用是把数据从executor上面拉取到driver上，所以使用的时候必须得注意不能拉取太大的数据，只拉取计算结果
那为什么会有把数据拉取回来的需求呢？因为有的时候数据得插入数据库，因为数据库有安全与性能问题所以选择在操作机上插入是比较合理的
saveAsTextFile这个保存成文件的算子，其实一点也不神秘，为什么呢？因为其底层使用的hadoop的outputformat完成的，所以说spark是有很多地方借鉴了hadoop的东西。
补充一下，不管是写Transformations和Action咱们的那部分代码是跑在集群上的呢？记住只有算子中的function是跑在集群上的（DataStream中的foreachRDD和transform除外）

transform : map   filter   flatMap   groupby   join   union    distinct   
action : count  collect  foreach   reduce   first   take     saveAsTextFile  


spark的共享变量分为两种
1.广播变量：在driver端产生，然后分发给所有的executor，这样所有的executor都可以在自己的机器上拿到要join的数据
一般用于替代算子里函数中使用的外部的大对象这样可以避免多次的网络传输，优化性能
spark的广播变量就相当于mapreduce的distributecache都是用于实现mapjoin的
一般什么样的东西要被变成广播变量？小表，小表一般是配置

2.累加器：在driver端产生，然后在task里面进行累加，也就是在集群上累加，之后在driver端进行汇总
一般用于统计使用，记住千万不能用spark算子中函数的外部变量来存储统计值，因为这个变量只在task里面加，不会在driver端进行汇总
spark的累加器相当于mapreduce的counter都是用于统计的



blockManagerMaster和blockManager是干吗的？
blockManagerMaster是在driver端保存rdd的元数据的，通常用于数据本地化时使用的
blockManager是管理exeuctor中的内存或者硬盘存数据的，并负责拉取其它executor数据的

spark的executor的内存修改管理
1.5之前的版本是默认手动的形式，1.5之后是自动的形式（但是可以通过spark.memory.useLegacyMode打开手动形式）
那什么时候要进行executor内存的修改呢？
比如说你不需要cache RDD的时候，也就是说你的所有内存都给你的计算任务的时候这样就可以把cache的内存变得很小


spark的shuffle优化：
1.首先要开启consolidation机制，这个在1.5之后自动开启的，之前一定要手动开启（spark.shuffle.consolidateFiles=true）
  主要目的可以减少map端的文件输出的数量从而减少磁盘IO和map端使用io对像的数量从而减少内存的使用
2.设置shuffle参数，增加下游拉取时候的等待时间和等待次数（spark.shuffle.io.maxRetries，spark.shuffle.io.retryWait）
3.设置map端输出的缓存和reduce端拉取的缓存（spark.shuffle.file.buffer，spark.reducer.maxSizeInFlight）
4.减少网络传输的数据，可以使用kryo来代替serializable，开启输出端的snappy压缩
5.使用合理的算子，比如说使用带combiner的算子，比如reducerByKey来代替groupByKey
6.尽量的少new对象这样的话jvm就不会频繁的full gc，这样shuffle就不会因为GC而暂停


spark可以使用自定义的对象当key和value进行网络传输
用于key必须实现Serializable和compareable
用于value必须实现Serializable

spark的排序其实是和java排序差不多都是使用的compare进行数据的对比

spark序列化方案有两种
1.kryo序列化之后的数据小，速度快，不适用于所有的java对象
2.serializable序列化之后的数据大，速度慢，适用于所有的java对象，它是默认的
建议使用kryo
kryo的用法
1.自动注册所有要序列化的类（推荐）
2.手动注册所有要序列化的类（不推荐，太麻烦）


JVM的GC原理
1.首先放入Eden和Survivor1（可能是短时间）的
2.当Eden满了会启动minor gc，回收新生代中不再使用的对象，还要用的就放到Survivor2中
3.移完之后eden和Survivor1中省下的就是不再使用的对象，就将他们清理掉
4.Survivor1和Survivor2交换角色。那就是原来的Survivor1成了备用的了，也就是原来的Survivor2
5.多次在Survivor区没有被清理掉的，说明它是长时间使用的，那么将它移动到老年代，到目前为止世界一切和平
6.由于对象越New越多，minor时发生备用的Survivor区满了，放不进去了，怎么办呢？这个本来可能是短时间生存的对象被放入老年代
7.短时间生存的对象，很可能快速的给老年代占满，白白的浪费老年代的空间，就会触发Full GC，回收老年代的对象

看这个原理说白了就是要控制你自己别老new对象，尽量用string来代替你的对象，因为String有常量池避免少new对象



spark操作hbase
批量写数据：
1.在算子的fuction中创建一次连接然后一次只写一条，但是不会重复使用这个连接
2.在算子的fuction中创建一次连接然后一次写一批，但是不会重复使用这个连接（最快）
3.使用TableOutputFormat创建一次连接然后一次写入一条，但是会重复使用这个连接

批量读数据：
1.使用TableInputFormat创建一次连接然后读数据（一次读多少取决于你的scan对象的配置    scan.setCaching(10000)，scan.setCacheBlocks(false)），但是会重复使用这个连接

对于spark操作hbase和mr操作hbase写入数据的时候一样都有个神器，就是生成hfile往里mv，这样原理是一样，而且能一次写入大量的数据


spark中yarn上的运行流程
首先得有一个HDFS目录存储jar包，因为各节点在跑任务的时候都会以去从这个目录里拿他们所需要的环境jar包，所以说spark在yarn上运行并不需要在每个节点上安装spark
怎么让你的jar包上传到HDFS呢？有2种方法
1.命令指定本地jar包目录，然后在启动任务之前会把本地的jar包上传到HDFS的一个临时目录（不推荐，因为每次启动任务都上传一便，这样会放任务的启动速度变慢）
2.要能通过在spark-defaults.conf配置存放jar包的hdfs目录，然后手动把jar包上传到hdfs指定的目录一次就可以了（推荐）

spark在yarn上的运行模式有两种
1.yarn-client用于测试，因为，driver运行在本地客户端，负责调整application,会与yarn集群产生超大量的网络通信，从而导致网卡流量太大，从而导致网络阻塞，可能就会导致任务失败，好处是在执行时，本地可以看到所有的log，方式调试。
2.yarn-cluster，用于生产环境，因为driver运行NM上，没有网卡流量的问题，缺点在于，调试不方便，本地用spark-submit提交之后，看不到log，只能通过yarn app logs这种命令查看，很麻烦


spark-sql能替代hive的全部功能吗？
几乎能替代，比如说共用元库，管理元库，窗口函数，自定义函数，与hiveServer2相同thriftServer服务，就是有时候相同的DDL语句在spark中可能会出错


spark-sql在集群上运行有两方法
1.自己的spark-sql环境，不能实现数据的共享，hive on spark是基于这种方式实现
2.共用spark-sql环境，可以实现数据的共享，相当于hive的hiveServer2服务器

怎么知道自己使用的RDD到底会使用多大的存储空间？
通过查看spark的webui看一下你的分区是否均衡，如果均衡，那executor设置多大的内存能把partition都放在executor的内存里。
1.首先看一个executor的cpu核数，有多少个cpu核就等于将要cache多少的partition
2.然后再大约的算一下每个partition的数据放在内存里面是多大，这个怎么算呢？一般是parition文件的大小乘以3就可以得到这个分区的数据加载到executor的jvm中的实际大小
3.得到partition在内存中的实际大小再乘以executor的cpu核数就是最终executor要这是的缓存内存大小

spark-sql在执行之前是会对你的SQL进行优化了，分成6步，然后每一步是啥自己去背一下。
根据sql语句生成执行计划
解析逻辑执行计划，把sql划分一个tree的结构
优化逻辑执行计划，优化语句（例如将where条件放到子查询中，减少数据查询量）
生成物理执行计划，读哪个文件，怎样关联，查询哪个列,如何扫描表和表的索引......
执行最终的逻辑执行

spark-sql可以使用api自己造表的结构
spark-sql还可以转成RDD，RDD也可以转成spark-sql，也就是说ds->rdd,df->rdd,rdd也可以转成它们两个，这样的话就牛逼了，因为可以在一个程序里面同时实现rdd开发与spark-sql开发
但是要注意一个隐式转换
import spark.implicits._
//这里的spark是SparkSession的变量名

rdd，ds，df的区别
1.都是弹性分布式数据集
2.rdd不支持SQL，ds和df支持
3.df是ds的泛型为row的特殊实现
4.它们可以互转
5.rdd的cache（内存）和ds,df的cache（硬盘）默认级别是不同的
6.ds和df的shuffl默认是200的partition

spark-streaming它就是批中的流（批量计算基础之上实现的流，因为底层都是spark-core），也可以说是流式计算中的批量计算，也就是说spark-streaming不是真正的流式运算
DataStream它就是一小批的数据，这小批的数据也可以划分成多个parition，然后分布式运算。spark-streaming会根据时间持续的产生DataStream

其实DataStream的api跟RDD很相似，是spark开发者为了减少大家的学习成本而故意这么做的，但是呢也有一些特殊的api
比如updateStateByKey，Windows，foreachRDD，transform
updateStateByKey使用的时候注意事项
1.必须有ckp
2.要结合getORcreate使用
3.不能把生成的第一个DataStream放在getORcreate的外面
4.支持你停止spark-streaming程序时重新修改你的算子中的函数

windows可以合并多个批次的数据，一般的使用场景是什么呢？比如你的领导让你每5分钟出过去一小的统计数据，那此时你的windows该怎么设置？
就要考虑windows的3个参数了：1.批次间隔  2.窗口大小  3.划动间隔
由于是5分钟统计一次，那新的窗口生成时间是5分钟，所以划动间隔是5分钟
由于要统计过去一小时的数据，所以窗口的大小是一小时
由于划动间隔和窗口大小分别是5分钟和一小时，那批次间隔怎么设置？应该小于划动和窗口的大小所以设置小于5分钟就行了，但是要注意保证你的划动间隔和窗口大小是批次间隔的整数倍，比如说设置成5秒或1分钟都可以。
别外还需要注意一点再使用窗口的时候你的划动间隔不能大于窗口的大小，否则就会丢数据


foreachRDD，transform这两个算子是spark-streaming开发中最常使用的，为什么呢？
因为它们可以把dataStream中的rdd拿出来，从而把spark-streaming的开发变成spark-core的开发，这么做有什么好处呢？变成spark-core开发咱们就可以实现rdd转成spark-sql开发了，从而你的spark-streaming开发就可以写SQL了，比如说使用窗口函数等等
这两个算子的区别，foreachRDD是action没有返回值，transform是transformation将返回其它的dataStream

常用的流有那些
1.socket流用于练习
2.file文件流用于加载配置
3.kafka队列流用于大量数据的正式开发
一般是kafka流与file流进行join用于在流式计算中拿到程序的配置，但是这么做有一点不好，那里不好，就是会产生shuffle。那怎么办呢？
咱们可以使用在流中更新广播变量的方式来代替file流的操作，从而达到mapjoin的方式，避免shuffle

spark-streaming中有一个特殊的东西那就是receiver，这个receiver不是所有的流都有的，比如file流或才kafkaDirece流就没有。
当有receiver的流比如socket需要注意一点，这个receiver是占用一个executor中的cpu的，所以有receiver的流的cpu在设置大于1。
这个在两个流合并的时候要格外注意，因为如果合并的是两个receiver流那cpu应该设置为大于2

kafka
produer
consumer
consumer group
broker
topic
Partition
offset
consumer group中的consumer读取topic中的数据每个Partition都有对应的offset

怎么保证topic中的partition数据是均衡的？由谁来保证？
由于放到那里是producer决定的，所以均不均衡是由producer来保证的
放数据的方式：
1.随机
2.轮训
3.按key的hashCode放
4.自己指定一个自定义的partitioner去放

producer往partition中放的是什么样的数据？
partition中的数据存的是二进制

consumer group中的consumer比topic中的partition少怎么办？
一个consumer可能读多个partition数据


consumer group中的consumer比topic中的partition多怎么办？
多余的consumer就会不干活


如何为Kafka集群选择合适的Topic/Partitions数量
好处：
1).越多的分区可以提供更高的吞吐量，因为一个Partitions可以对应一个Consumer，Partitions越多吞吐量越高，也就是Consumer会并行读取Partitions
环处：
2).分区多了会消耗更多的系统资源比如文件的句柄，broker会为每个Partitions分配一个文件目录，目录中分索引文件和数据文件。所以分区多了需要打开的文件自然就多了。
3).更多分区会导致更高的不可用性，在有副本的情况下更多的分区会导致更多的复制操作，在brokers宕机时也需要恢复更多的Partition
4).越多的分区复本可能增加端对端的延时，因为producer发布消息后consumer需要等待所有partition完成复本复制之后才能得到该消息，分区复本越多说明复制所花费的时间越长，因为从一个broker到别一个broker复制数据是由一个线程来完成的
5).越多的Partition意味着需要客户端分配更多的内存，比如一个topic有2000个分区，当只有一个consumer时，则这个consumer需要在内存中记住这2000个分区信息

那该怎么设置：
1.首先一个分区大约每秒能处理2000条数据
2.考虑完第一点，还要考虑的就是下游的并发度，比如你的业务比较复杂然后按处理的数据量去算分区数的时候consumer是处理不过来的，因为consumer比分区多的时候有很多consumer可能会是闲的，这样就导致忙的consumer忙不过来，闲的又不干活，主要原因是分区数不够，那这个时间就不能以处理的数据量来定分区数了，要以下游的consumer的数量来定，也就是多少consumer能处理好这些数据，那分区数就设多少。
3.切记需要设置为比上限(kafaka分区数与executor的数量)多10%就行，因为需要10%的冗余，避免突然的数据峰值。


kafka的参数优化主要看那，就是看各种buffer的大小，和线程数

由于kafka的命令很多，又很烦人，所以咱们可以安装一个yahoo开源的kafka-manager来管理kafka

kafka的api需要掌握什么，producer如何自定义partitioner，因为这个能实现只往某一个partition中插入数据，以达到消费有序
还需要掌握conusmer怎么提交offset的，自动提交与手动提交，可以通过参数(enable.auto.commit)来进行设置

consumer消费topic的启动模式要了解分为三种：earliest和latest，从group的offset位置消费

spark-streaming消息kafka会问你什么是直连模式什么是普通模式（receiver）
直连是kafka0.10之后的功能，是直接连接到kafka的partition上，不需要receiver做中转，因为receiver使用内存存数据，内存满了还得WAL，这样即不安全性能又差，所以干脆在后续的版本中给干掉了

直连模式的数据本地化：
1.在broker的机器上运行spark-streaming程序（发果条件满足推荐用这个）
2.在任意机器上运行spark-streaming程序，然后通过网络拉取kafka的数据（大多数情况下用这个）

spark-streaming读取kafka最重要的事是啥呢？是保存partition的offset，因为操作kafka这个offset就是十分重要的东西，如果这个东西丢了，那数据就可能处理丢失或者重复。
所以一定要给他保存到一个靠谱的地方，比如zk，mysql集群，redis集群。这里选择那个都行，但是一定是集群模式的因为安全。

那kafka自己也能保存offset为什么还有zk集群来保存呢？
为了防止kafka太忙然后offset不能即时保存，所以采用了别的靠谱存储系统让offset的保存不依靠kafka，以达到解耦的目的。


怎么处理zk中保存的offset太老的问题，与kafka中的parition最早的offset进行对比啊，哪个更大用那个

把spark优化汇总的内容看3便。


