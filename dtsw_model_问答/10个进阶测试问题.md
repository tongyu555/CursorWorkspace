# AI Models 进阶业务测试问题清单（需解析思考）

1. **查看指标来源（多条件推理）**：我想分析4G网络中，因为重叠覆盖导致的问题，并且我需要看每天的数据情况，请问我应该关联查询哪几张表？
2. **查看表血缘（链路推理）**：如果 `fact_lte_mro_usrbackfill_q`（4G电联MRO用户回填详单表）的数据出现延迟，会直接或间接影响到哪些“天”级别的覆盖分析汇总表？请列出受影响的最终天表。
3. **查看表清单2（组合特征查询）**：请帮我梳理出所有用于“高铁优化”场景，并且计算位置在“前置”的模型表清单。
4. **统计表数量（多维统计）**：在“覆盖分析”主题域中，生命周期为30天的表和生命周期为14天的表分别有多少张？
5. **检索相似模型（业务意图解析）**：业务部门想要评估某个区域内用户的聚集情况和常驻情况，以天为单位进行观察，系统中有哪些模型表可以满足这个需求？它们之间有什么区别？
6. **查看表出度（核心节点识别）**：在所有“整合层”的表中，哪一张表作为前向表被下游模型依赖调用的次数最多？它主要支撑了哪些主题域的分析？
7. **查看表属性（规则校验）**：请帮我检查一下，所有“汇聚维度”包含“栅格”的模型表，它们的“是否同步”属性通常是怎么设置的？有没有规律？
8. **查看指标来源（跨层级溯源）**：如果我发现 `aggr_lte_coverage_cel_refactor_d`（4G MR小区覆盖指标天表）的数据异常，我需要一步步往上游排查，请告诉我它的完整数据溯源链路（直到贴源层或外部数据源）。
9. **检索相似模型（场景区分）**：同样是看“用户小区覆盖”，`aggr_lte_coverage_usrcel_refactor_d` 和 `aggr_lte_iov_coverage_usrcel_h` 这两张表在应用场景和数据粒度上有什么本质区别？
10. **查看表清单2（特定系统支撑）**：我想知道“建维优”系统目前主要依赖了我们中间层的哪些模型表？请列出清单并说明这些表的数据时间粒度。